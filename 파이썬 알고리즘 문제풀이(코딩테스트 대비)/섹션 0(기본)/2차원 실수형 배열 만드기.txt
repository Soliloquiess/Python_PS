def asdf(arr):


  return list(map(float, arr))

arr = [
  [0 for col in range(2)]
  for row in range(3)
]

arr = list(map(asdf, arr))

for i in arr:
  for j in i:  # 안쪽 리스트에서 요소를 하나씩 꺼냄
    print(j, end=' ')